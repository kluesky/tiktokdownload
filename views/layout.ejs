<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Chisato TikTok Downloader</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background: url('https://files.catbox.moe/9n8wrx.jpg') no-repeat center center fixed;
      background-size: cover;
    }
    .glass {
      background-color: rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.15);
      box-shadow: 0 0 30px rgba(56, 189, 248, 0.2);
    }
  </style>
</head>
<body class="text-white font-sans min-h-screen flex items-center justify-center p-4">
  <div class="glass p-6 rounded-2xl w-full max-w-xl text-center">
    <h1 class="text-3xl font-bold text-cyan-400 mb-4 tracking-wide">📥 TikTok Downloader</h1>

    <form action="/download" method="POST" class="mb-4">
      <input type="text" name="url" placeholder="Masukkan link TikTok..."
        class="w-full p-3 rounded-lg bg-white bg-opacity-10 border border-cyan-300 focus:outline-none focus:ring-2 focus:ring-cyan-500 text-white placeholder-gray-300" required />
      <button type="submit"
        class="mt-4 w-full py-2 rounded-lg bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-blue-600 hover:to-cyan-600 font-semibold transition shadow-lg">🎬 Download Biasa</button>
    </form>

    <!-- Tombol Donasi -->
    <div class="mb-6">
      <a href="https://trakteer.id/kyluesky/tip" target="_blank"
         class="inline-block px-4 py-2 bg-pink-500 hover:bg-pink-600 rounded-full shadow-lg text-white font-semibold transition">
        ❤️ Dukung Web Ini di Trakteer
      </a>
    </div>

    <% if (result) { %>
      <% if (result.error) { %>
        <p class="text-red-400 text-sm font-semibold"><%= result.error %></p>
      <% } else { %>
        <p class="italic text-sm text-gray-300 mb-4">"<%= result.desc %>"</p>
        <img src="<%= result.thumb %>" alt="Thumbnail"
          class="mx-auto rounded-xl shadow-lg border border-cyan-400 mb-4 w-64" />

        <!-- Download Biasa (No login required) -->
        <a class="w-full block bg-blue-400 hover:bg-blue-500 text-black font-semibold py-2 rounded-lg transition shadow mb-3"
           href="/download-file?url=<%= result.wmplay %>&type=mp4">⬇️ Download Biasa (dengan watermark)</a>

        <!-- Download HD (Login Only) -->
        <% if (session?.user) { %>
          <a class="w-full block bg-cyan-500 hover:bg-cyan-600 text-black font-semibold py-2 rounded-lg transition shadow"
             href="/download-file?url=<%= result.video %>&type=mp4">💎 Download HD (tanpa watermark)</a>

          <a class="w-full block bg-yellow-400 hover:bg-yellow-500 text-black font-semibold py-2 rounded-lg transition shadow mt-3"
             href="/download-file?url=<%= result.music %>&type=mp3">🎵 Download MP3</a>

          <p class="mt-4 text-sm text-gray-400">Login sebagai <%= session.user %> | <a href="/logout" class="underline text-cyan-300">Logout</a></p>
        <% } else { %>
          <p class="text-sm text-gray-300 mt-4 italic">🔒 Download HD & MP3 hanya untuk pengguna login</p>
          <a href="/login"
             class="mt-2 inline-block bg-gray-700 hover:bg-gray-600 px-4 py-2 rounded-lg font-semibold text-white transition">Login untuk akses VIP</a>
        <% } %>
      <% } %>
    <% } %>

    <p class="text-xs text-gray-400 mt-8">© 2025 Chisato AI • Powered by TikWM</p>
  </div>
</body>
</html>